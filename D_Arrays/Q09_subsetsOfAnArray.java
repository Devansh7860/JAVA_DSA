package D_Arrays;

public class Q09_subsetsOfAnArray {
    // all subsets of an array
    // ---------------------------------------------------------------------------
    // there are total 2^n subsets where n = no. of elements
    public static void main(String[] args) {
        int[] arr = {1,2,3};
        int lim = (int)Math.pow(2 , arr.length);
        for(int i = 0; i<lim; i++){
            
        }
    }
}
